<link rel="stylesheet" type="text/css" href="style.css">
<% @players.collect do |player| %>
<% if player["team"]["Abbreviation"].eql? params[:id] %>
<h1><%= link_to player["team"]["Name"], "/home"  %></h1>
    <% break %>
  <% end %>
  <% end %>
<div class="content">
  <table>
    <tr>
    <th>
    Player
    </Th>
    <Th>
    Number
    </Th>
  </Tr>
    <% @players.collect do |player| %>
    <% if player["team"]["Abbreviation"].eql? params[:id] %>
      <Tr>
        <td>
          <%= link_to "#{player['player']['FirstName']} #{player['player']['LastName']}", ('https://en.wikipedia.org/wiki/' + player["player"]["FirstName"] + "_" +  player["player"]["LastName"])%>

        </td>
        <td>

          <%= player["player"]["JerseyNumber"]%>

        </td>
      </Tr>
    <% end %>
    <% end %>
 </Table>
 </div>
